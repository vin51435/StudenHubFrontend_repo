export const POST_SORT_OPTIONS = ['Hot', 'New', 'Top', 'Controversial'];
export type PostSortOption = (typeof POST_SORT_OPTIONS)[number];

export const TIME_RANGE_OPTIONS = ['now', 'today', 'week', 'month', 'year', 'all'];
export type TimeRangeOption = (typeof TIME_RANGE_OPTIONS)[number];
